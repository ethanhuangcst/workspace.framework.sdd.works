# Issues log — framework.sdd.works

> Open defects and their closure. Category is one word. Status is `Open` or `Closed`.
> Related specs: [`product-backlog.md`](./product-backlog.md) · [`admin-portal/app-stories.md`](./admin-portal/app-stories.md)

## Web-app


| Id | Title | Description | Related | Status | Closed check |
| --- | --- | --- | --- | --- | --- |
| WA-01 | `/` is still the logo-card home | Requirement: `/` is the instructions guide (change-log feature-10). Live `src/app/page.tsx` still renders `HomePage`. | [Web-portal-09](./product-backlog.md#pb-76) | Closed | `e2e/auth.spec.ts` `should_show_instructions_guide_on_root`: `/` shows `instructions-guide`, no logo-card home controls |
| WA-02 | Footer scrolls away | Footer is in normal document flow. It should stay fixed at the bottom of the viewport on auth and admin shells. | [Web-portal-09](./product-backlog.md#pb-76) | Closed | Same test: `.site-footer` stays in viewport after scroll to bottom |
| WA-03 | Reset success still says Back to home | After the reset mail is sent, the link should be Back to login → `/login`. | [Web-portal-09](./product-backlog.md#pb-76) | Closed | `should_show_back_to_login_after_reset_mail_sent`: `reset-back-login` → `/login` |
| WA-04 | Empty-password screen traps accounts that already have a password | `me@ethanhuang.com` keeps seeing “This account has no password yet…” after a password was set. Login redirects to set-password when the session or gate is wrong. E2E global-setup overwrites the seed hash. | [Web-portal-09](./product-backlog.md#pb-76) | Closed | `should_redirect_hashed_admin_away_from_empty_set_password` + `should_land_on_keys_when_credentials_correct`; empty-hash path still reaches set-password |
| WA-05 | Reset submit flashes and stays on the form | Enter email, Send reset mail: page flashes, URL stays `/reset-password`, form returns, no mail / no success callout. Client must keep the document from navigating; failed send must show a keyed error. | [Web-portal-10](./product-backlog.md#pb-77) | Closed | Browser + `should_show_back_to_login_after_reset_mail_sent`: URL stays `/reset-password`, success callout, form hidden; unit covers failed request → `reset-error` |
| WA-06 | Features tab cannot be clicked | On the instructions guide (`/` or `/instructions`), the Features tab does not open the features panel. Tab must be the hit target and switch the panel. | [Web-portal-10](./product-backlog.md#pb-77) | Closed | Features is a link to `?tab=features`. Playwright `should_switch_features_tab_and_be_hit_target` plus browser click: URL `/?tab=features`, panel shown, setup panel `display: none` |
| WA-07 | Reset claims success while Resend is skipped | Dev server left with Playwright capture env: submit shows success, lead stays, inbox empty. Skip Resend only when `E2E_SKIP_MAIL=1`; hide lead after success. | [Web-portal-10](./product-backlog.md#pb-77) | Closed | Reset/invite gate on `E2E_SKIP_MAIL`; Playwright `reuseExistingServer: false`; unit hides lead on success; browser reset on clean `npm run dev` uses Resend |
| WA-08 | Reset success does not name the email and can disappear | After Send reset mail, the visitor should see a callout that names the address (`admin.reset.sent` with `{email}`). A document GET to `/reset-password?email=` reloads the form and drops the success state. | [Web-portal-11](./product-backlog.md#pb-79) | Closed | `type="button"` + callout with `{email}`; unit asserts address; browser: success names email, URL has no `?email=` |
| WA-09 | Get secret leaves Features and never shows a value | On Features, Get secret navigates to Setup at the top of the page. No code block with the key value and no not-found notice. Lookup must stay on Features `#features-secret` and use exact `key_name`. | [Web-portal-08](./product-backlog.md#pb-74) | Closed | `POST /api/sdd/secret`; `type="button"`; unit found/missing/empty; Playwright stay on Features + not-found; browser: not-found stays on `?tab=features` |
| WA-10 | Reset success copy and local mail | Success callout must use the previous `admin.reset.sent` (no `{email}`). Local clicks returned `200` in tens of ms with no Resend call (non-ACTIVE admin or capture mode); production still sends. See [admin-portal-seed-and-logo.md](./knowledge/agent/admin-portal-seed-and-logo.md) and [web-app-fix-regression.md](./knowledge/agent/web-app-fix-regression.md). | [Web-portal-11](./product-backlog.md#pb-79) | Closed | Catalog restored in `en` / `zh-Hans` / `zh-Hant`; callout has no `{email}`; unit expects previous sentence; reset route logs skip / Resend-accepted without URL; browser success shows previous sentence |
| WA-11 | Get secret result not in view; found layout incomplete | After Get secret, the result (code block or not-found) must scroll into view. Found value must be a code block with a copy control, width equal to the secret name input plus the Get secret button. | [Web-portal-08](./product-backlog.md#pb-74) | Closed | Result scrolls after render (`block: "start"`); found uses `.codeblock` + `CopyButton`; width matches lookup row; unit + browser not-found on `?tab=features` |
| WA-12 | Local DB missing seed admin; empty-hash test rows force set-password | Local only. Production is unaffected: the seeded admin exists there with a password, so login checks the password and reset calls Resend. Local Postgres has no `me@ethanhuang.com` row. Reset for that address returns `{ ok: true }` and does not call Resend. Login is 401. Playwright `should_send_empty_password_admin_to_set_password` inserted `empty-*` admins with `passwordHash: ""` into this same database and did not delete them. Login treats `""` as no password, skips verification, and `/login` plus `requireAdmin()` keep redirecting to `/set-password`. `POST /api/admin/password/set` updates only the session admin, so saving a password cannot create the missing seed account. **Solution:** delete those leftover `empty-*` rows; make that test delete its fixture admin when it finishes; run `npx prisma db seed` so the seed admin exists with a hash (do not edit `.env` / `.env.local`, and do not overwrite a non-empty hash). Leave the empty-hash gate as it is. | [Web-portal-09](./product-backlog.md#pb-76) | Closed | Deleted leftover `empty-*` rows; `e2e/auth.spec.ts` deletes fixture in `finally`; seed created `me@ethanhuang.com` with hash set; `prisma/seed.ts` keeps an existing non-empty hash |
